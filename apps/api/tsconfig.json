{
  "extends": "../../tsconfig.base.json",
  "comment": "Inherits module/moduleResolution NodeNext (ESM) from the base — matches the .js-extension import style the source already uses, and lets ESM-only deps (jose) resolve via their exports map. See ADR-0014.",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist",
    "declaration": false,
    "declarationMap": false
  },
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules", "dist", "**/*.spec.ts"]
}
