/**
 * Public surface of the auth feature-slice. Sign-in screen, session seeding, and cosmetic
 * RBAC live here as they are built. Auth client state is the store-level `authSlice`
 * (src/store/slices/authSlice.ts); the Supabase client lives in src/lib/auth.ts.
 *
 * Guide: auth.md. Empty until the sign-in screen and session seeder land.
 */
export {};
