/**
 * Public surface of the employees feature-slice (admin management + encode-form typeahead).
 * Transport is src/services/employees.service.ts; de-dup is enforced server-side (M4).
 *
 * Guide: employees.md. Empty until the screens land.
 */
export {};
