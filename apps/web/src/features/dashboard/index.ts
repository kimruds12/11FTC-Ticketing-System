/**
 * Public surface of the dashboard feature-slice (analytics). Read-only; aggregates are
 * computed server-side (M9) and read in a Server Component — no Redux, no client fetch.
 *
 * Guide: dashboard.md. Empty until the dashboard screen lands.
 */
export {};
